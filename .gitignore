# OS files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Editor directories and files
.vscode/
.idea/
*.swp
*.swo
*~

# Node modules (if any utilities use Node)
node_modules/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Temporary files
tmp/
temp/
*.tmp

# Yoyo Dev Project Data - ENTIRE directory ignored
# The BASE framework repository should NEVER contain a .yoyo-dev directory
# .yoyo-dev/ is created during PROJECT installation, not in the framework source
# This prevents project-specific data from contaminating the distributed framework
.yoyo-dev/

# Local environment files
.env
.env.local
.env.*.local

# Cache
.cache/

# Build outputs
dist/

# Claude Code Local Configuration
# .claude/ contains local settings, skills, and customizations
# Each developer has their own local Claude Code setup
# The canonical source for agents/commands/hooks is in the yoyo-dev framework itself
.claude/
